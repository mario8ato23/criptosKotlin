package com.example.tradingcriptos.tutorial

// InputArray [3, 6, -2, -5, 7, 3]


fun adjacentProduct(inputArray: Array<Int>) {
    inputArray.forEachIndexed { index, i ->

    }
    val maxProduct = inputArray[0] * inputArray[1];

    //
    for (a in inputArray) {

    }
}